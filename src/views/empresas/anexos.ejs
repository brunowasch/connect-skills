<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Anexos - Connect Skills</title>

  <!-- Bootstrap / Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">

  <!-- Estilos do projeto -->
  <link rel="stylesheet" href="/css/shared/style.css">
  <link rel="icon" type="image/png" href="/img/CONNECT.png?v=2">

  <style>
    .card-upload { border: 1px dashed #ccc; border-radius: 12px; }
    .muted { color: #6c757d; }
    .table td, .table th { vertical-align: middle; }
  </style>
</head>
<body class="bg-light">
  <%- include('../partials/header-empresa') %>
  <%- include('../partials/flash-messages') %>

  <main class="container my-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
      <h1 class="h4 m-0">Anexos da Empresa</h1>
      <a href="/empresa/meu-perfil" class="btn btn-light">Voltar</a>
    </div>

    <% if (erro && erro.length) { %>
      <div class="alert alert-danger"><%= erro.join('<br>') %></div>
    <% } %>
    <% if (msg && msg.length) { %>
      <div class="alert alert-success"><%= msg.join('<br>') %></div>
    <% } %>

    <div class="row g-3">
      <div class="col-12 col-lg-6">
        <div class="card card-upload p-3 h-100">
          <h2 class="h6">Enviar arquivos</h2>
          <p class="muted mb-2">Formatos aceitos: imagens, PDF, DOC/DOCX etc. (máx. 10MB/arquivo)</p>

          <!-- Form próprio (não aninhado) -->
          <form action="/empresa/anexos/upload" method="post" enctype="multipart/form-data" class="d-flex gap-2 flex-wrap">
            <input class="form-control" type="file" name="arquivos" multiple />
            <button class="btn btn-primary">Enviar</button>
          </form>
        </div>
      </div>

      <div class="col-12 col-lg-6">
        <div class="card p-3 h-100">
          <h2 class="h6">Adicionar link</h2>
          <p class="muted mb-2">Cole uma URL iniciando com <code>http://</code> ou <code>https://</code>.</p>

          <!-- Form próprio (não aninhado) -->
          <form action="/empresa/anexos/link" method="post" class="row g-2">
            <div class="col-md-5">
              <label class="form-label small">Rótulo (opcional)</label>
              <input class="form-control" type="text" name="label" placeholder="Ex: Portfólio no Drive" />
            </div>
            <div class="col-md-5">
              <label class="form-label small">URL</label>
              <input class="form-control" type="url" name="url" placeholder="https://..." required />
            </div>
            <div class="col-md-2 d-grid">
              <label class="form-label small invisible">.</label>
              <button class="btn btn-secondary">Salvar link</button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <hr class="my-4" />

    <div class="card p-3">
      <div class="d-flex justify-content-between align-items-center">
        <h2 class="h6 mb-0">Meus anexos (<%= anexos?.length || 0 %>)</h2>
      </div>

      <div class="table-responsive mt-3">
        <table class="table align-middle">
          <thead>
            <tr>
              <th>Nome</th>
              <th>Tipo</th>
              <th>Tamanho</th>
              <th>Enviado em</th>
              <th class="text-end">Ações</th>
            </tr>
          </thead>
          <tbody>
            <% if (!anexos || !anexos.length) { %>
              <tr>
                <td colspan="5" class="text-center text-muted">Nenhum anexo enviado ainda.</td>
              </tr>
            <% } else { %>
              <% anexos.forEach(a => { %>
                <tr>
                  <td class="text-truncate" style="max-width: 360px;"><%= a.nome %></td>
                  <td><code><%= a.mime %></code></td>
                  <td><%= typeof humanFileSize === 'function' ? humanFileSize(a.tamanho) : ((a.tamanho||0) + ' B') %></td>
                  <td><%= new Date(a.criadoEm).toLocaleString('pt-BR') %></td>
                  <td class="text-end">
                    <a href="<%= a.url %>" target="_blank" rel="noopener" class="btn btn-sm btn-outline-primary">Abrir</a>
                    <form action="/empresa/anexos/<%= a.id %>/delete" method="post" class="d-inline" onsubmit="return confirm('Excluir este anexo?');">
                      <button class="btn btn-sm btn-outline-danger">Excluir</button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>
  </main>

  <%- include('../partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
