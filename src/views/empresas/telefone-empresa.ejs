<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Connect Skills</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/shared/style.css">
  <link rel="stylesheet" href="/css/shared/telefone.css">
  <link rel="icon" href="/img/CONNECT.png">
</head>
<body>
  <%- include('../partials/header') %>

  <main class="d-flex flex-column align-items-center justify-content-center mt-5">
    <h3 class="text-center mb-4">Digite seu número de telefone:</h3>

    <form action="/empresa/telefone" method="POST" id="formTelefone" novalidate>
      <!-- Campo oculto com o ID do usuário -->
      <input type="hidden" name="usuario_id" value="<%= usuario_id %>">

      <div class="form-card">
        <div class="mb-4">
          <div class="d-flex justify-content-center align-items-center gap-2 flex-wrap">

            <!-- DDI fixo -->
            <div class="d-flex flex-column align-items-start">
              <input type="text" class="form-control text-center" name="ddi" id="ddi" value="+55" readonly style="width: 60px;">
            </div>

            <!-- DDD -->
            <div class="d-flex flex-column align-items-start">
              <input type="text" class="form-control text-center" name="ddd" id="ddd" maxlength="2" placeholder="DDD" style="width: 70px;" required autocomplete="tel-area-code" aria-label="DDD">
              <div class="invalid-feedback text-danger">Informe um DDD válido com 2 dígitos.</div>
            </div>

            <!-- Número -->
            <div class="d-flex flex-column align-items-start">
              <input type="text" class="form-control text-center" name="telefone" id="telefone" maxlength="15" placeholder="Número" style="width: 180px;" required autocomplete="tel-national" aria-label="Telefone">
              <div class="invalid-feedback text-danger">Informe um número válido entre 8 e 11 dígitos.</div>
            </div>
          </div>
        </div>

        <div class="d-grid mt-4">
          <button type="submit" class="btn btn-primary btn-lg">Continuar</button>
        </div>
      </div>
    </form>
  </main>

  <%- include('../partials/footer') %>
  <script src="/js/shared/numeroTelefone.js"></script>
</body>
</html>
