<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Foto de Perfil - Connect Skills</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/shared/style.css">
  <link rel="stylesheet" href="/css/shared/fotoPerfil.css">
</head>
<body>
  <%- include('../partials/header') %>

  <main class="main-ajustado-header d-flex flex-column align-items-center justify-content-center px-3 text-center mb-3">
    <img src="/img/avatar.png" alt="Avatar provisório" width="150" height="150">
    <h3 class="text-center mt-5 mb-4">Adicione uma foto de perfil</h3>

    <!-- FORMULÁRIO CORRETAMENTE POSICIONADO -->
    <form action="/empresa/foto-perfil" method="POST" id="formFoto" class="text-center" enctype="multipart/form-data">

      <!-- Campo oculto necessário para persistência -->
      <input type="hidden" name="usuario_id" value="<%= usuario_id %>">
      <input type="hidden" name="fotoBase64" id="fotoBase64">

      <!-- Prévia da Foto -->
      <img src="/img/avatar.png" alt="Prévia da Foto" id="previewFoto" class="foto-icon">

      <!-- Upload e Captura -->
      <div class="d-flex gap-4 flex-wrap justify-content-center botoes-container" id="opcoesFoto">
        <label class="upload-btn" for="upload">📁 Fazer upload</label>
        <input type="file" id="upload" name="foto" accept="image/*" class="hidden-input">

        <button type="button" class="upload-btn" id="tirarFotoBtn">📷 Capturar agora</button>
      </div>

      <video id="video" autoplay playsinline style="display: none;"></video>
      <canvas id="canvas" style="display: none;"></canvas>
      <img id="previewFoto" class="rounded-circle mt-3" style="max-width: 200px; display: none;" />

      <p id="erroFoto" class="text-danger fw-semibold mt-2" style="display: none;">
        Por favor, selecione ou tire uma foto antes de continuar.
      </p>

      <!-- Ações após a foto -->
      <div id="acoesFoto" class="acoesFoto flex-wrap justify-content-center gap-3 mt-4" style="display: none;">
        <button type="button" class="btn-acao btn-cancelar" id="outraFoto">Tirar outra</button>
        <button type="submit" class="btn-acao btn-confirmar">Confirmar e continuar</button>
      </div>
    </form>
  </main>

  <%- include('../partials/footer') %>
  <script src="/js/shared/fotoPerfil.js"></script>
</body>
</html>
