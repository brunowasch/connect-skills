<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Connect Skills</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/shared/style.css">
  <link rel="stylesheet" href="/css/shared/fotoPerfil.css">
  <link rel="icon" type="image/png" href="/img/CONNECT.png?v=2">
</head>
  <style>
    :root { --nav-h: 0px !important; }
    body#cadastro-foto { margin: 0; padding-top: 0 !important; }
    .main-ajustado-header { margin-top: 0 !important; padding-top: 0 !important; }
  </style>
<body id="cadastro-foto">
  <%- include('../partials/header') %>
  <%- include('../partials/flash-messages') %>
  <main class="main-ajustado-header d-flex flex-column align-items-center justify-content-center px-3 text-center mb-3">
    <h3 class="text-center mt-5 mb-4">Adicione uma foto de perfil</h3>

    <form action="/empresa/foto-perfil" method="POST" enctype="multipart/form-data" id="formFoto">
      <input type="hidden" name="usuario_id" value="<%= usuario_id %>">
      <input type="hidden" name="fotoBase64" id="fotoBase64">

      <img src="/img/avatar.png" alt="Prévia da Foto" id="previewFoto" class="foto-icon">

      <div class="d-flex gap-4 flex-wrap justify-content-center botoes-container" id="opcoesFoto">
        <label class="upload-btn" for="novaFoto">
          <i class="bi bi-upload"></i> Fazer upload
        </label>
        <input type="file" id="novaFoto" name="novaFoto" accept="image/*" class="hidden-input">

        <button type="button" class="upload-btn" id="tirarFotoBtn">
          <i class="bi bi-camera"></i> Capturar agora
        </button>
      </div>

      <video id="video" autoplay playsinline style="display: none;"></video>
      <canvas id="canvas" style="display: none;"></canvas>
      <img id="previewFoto" class="rounded-circle mt-3" style="max-width: 200px; display: none;" />

      <p id="erroFoto" class="text-danger fw-semibold mt-2" style="display: none;">
        Por favor, selecione ou tire uma foto antes de continuar.
      </p>

      <div id="acoesFoto" class="acoesFoto flex-wrap justify-content-center gap-3 mt-4" style="display: none;">
        <button type="button" class="btn-acao btn-cancelar" id="outraFoto">
          <i class="bi bi-arrow-clockwise me-1"></i> Tirar outra
        </button>
        <button type="submit" class="btn-acao btn-confirmar">
          <i class="bi bi-check-circle me-1"></i> Confirmar e continuar
        </button>
      </div>
    </form>
    <div class="mt-4">
        <a href="/empresas/home?usuario_id=<%= usuario_id %>" class="btn btn-outline-secondary">
        <i class="bi bi-skip-forward-fill me-1"></i> Pular esta etapa
      </a>
    </div>
  </main>

  <%- include('../partials/footer') %>
  <script src="/js/shared/fotoPerfil.js"></script>
</body>
</html>