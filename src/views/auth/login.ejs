<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Connect Skills</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/shared/style.css">
  <link rel="stylesheet" href="/css/login/login.css">
  <link rel="icon" type="image/png" href="/img/CONNECT.png?v=2">
</head>
<body>
<%- include('../partials/flash-messages') %>
<%- include('../partials/header') %>

<main class="container d-flex justify-content-center align-items-center flex-grow-1 main-ajustada">
  <div class="login-box shadow p-4 rounded-4 bg-white" style="width: 100%; max-width: 400px;">
    <div class="text-center mb-3">
      <h3 class="login-title mb-3">Faça seu login</h3>
    </div>
    <form action="/usuarios/login" method="POST">
      <div class="mb-3">
        <label for="email" class="form-label">E-mail</label>
        <input type="email" class="form-control" id="email" name="email" placeholder="Digite seu e-mail" required>
      </div>

      <div class="mb-3">
        <label for="senha" class="form-label">Senha</label>
        <div class="input-group">
          <input type="password" class="form-control" id="senha" name="senha" placeholder="Digite sua senha" required>
          <button type="button" class="btn btn-outline-primary" id="toggleSenha">
            <i class="bi bi-eye" id="iconSenha"></i>
          </button>
        </div>
        <div id="erro-senha-requisitos" class="text-danger mt-1" style="display: none;">
          A senha deve ter no mínimo 8 caracteres e pelo menos 1 caractere especial.
        </div>
      </div>

      <% if (erro) { %>
        <div class="alert alert-danger text-center mx-auto px-3 py-2" style="font-size: 0.95rem;">
          <%- erro %>
        </div>
      <% } %>

      <div class="d-flex justify-content-between align-items-center mb-3">
        <div class="form-check">
          <input class="form-check-input" type="checkbox" id="remember" name="remember" value="on">
          <label class="form-check-label" for="remember">Lembrar de mim</label>
        </div>
        <a href="/usuarios/recuperar-senha" class="text-decoration-none">Esqueceu sua senha?</a>
      </div>

      <div class="d-grid">
        <button type="submit" class="btn btn-primary">Entrar</button>
        <p class="mt-2">Não tem uma conta? <a href="/cadastro" class="text-primary ">Cadastre-se aqui</a></p>
      </div>

      <div class="d-flex align-items-center my-3">
        <hr class="flex-grow-1 border-secondary">
        <span class="mx-2 text-secondary">ou</span>
        <hr class="flex-grow-1 border-secondary">
      </div>

      <!-- Botão Google login -->
      <div class="mb-2">
        <a href="/auth/google?state=login" class="btn btn-outline-danger w-100 d-flex align-items-center justify-content-center gap-2 rounded-pill">
          <i class="bi bi-google"></i>Entrar com Google
        </a>
      </div>
    </form>
  </div>
</main>

<%- include('../partials/footer') %>

<script src="/js/shared/confirmarSenha.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>