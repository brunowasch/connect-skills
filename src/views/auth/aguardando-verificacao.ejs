<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Verifique seu E-mail - Connect Skills</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="icon" type="image/png" href="/img/CONNECT.png?v=2">
  <style>
    body {
      background-color: #f5f7fa;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .verificacao-box {
      background-color: white;
      border-radius: 12px;
      padding: 40px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      text-align: center;
      max-width: 480px;
      width: 100%;
    }

    .verificacao-box h2 {
      margin-bottom: 20px;
    }

    .verificacao-box p {
      margin-bottom: 15px;
    }

    .verificacao-box .btn {
      margin-top: 15px;
    }

    .email-destino {
      font-weight: bold;
      color: #0d6efd;
    }

    .logo {
      max-width: 100px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>

  <div class="verificacao-box">
    <% if (reenviado === 'true') { %>
        <div class="alert alert-success" role="alert">
            <i class="bi bi-check-circle-fill me-1"></i> Novo e-mail de verificação enviado com sucesso!
        </div>
    <% } %>
    <img src="/img/CONNECT.png" alt="Logo Connect Skills" class="logo">
    <h2><i class="bi bi-envelope-paper me-2"></i>Verifique seu e-mail</h2>
    <p>Enviamos um link de verificação para:</p>
    <p class="email-destino"><%= email %></p>

    <form action="/usuarios/reenviar-email" method="POST">
      <input type="hidden" name="email" value="<%= email %>">
      <button type="submit" class="btn btn-outline-primary w-100">
        <i class="bi bi-arrow-repeat me-1"></i>Reenviar e-mail
      </button>
    </form>

    <p class="mt-3 text-muted">Abra sua caixa de entrada e clique no link enviado.</p>
    <p class="text-muted">Se não encontrar, verifique sua pasta de spam ou lixo eletrônico.</p>

    <a href="/login" class="btn btn-link mt-2">
      <i class="bi bi-box-arrow-in-right me-1"></i>Ir para o login
    </a>
  </div>
<script>
  // Pega os elementos do HTML (ajuste os seletores/IDs se necessário)
  const titulo = document.querySelector('h2'); 
  const subtitulo = document.querySelector('.email-destino');
  const formBox = document.querySelector('.verificacao-box form');
  const linksBox = document.querySelector('.verificacao-box .btn-link');

  const poll = setInterval(async () => {
    try {
      // Pergunta ao servidor (usando o email do EJS)
      const response = await fetch('/usuarios/status-verificacao?email=<%= email %>');
      const data = await response.json();

      // ✅ O Servidor fez o login e nos deu a URL segura
      if (data.verificado && data.redirectTo) {
        clearInterval(poll);
        
        // Atualiza a tela para "Redirecionando..."
        if (titulo) titulo.innerText = 'Verificado! Redirecionando...';
        if (subtitulo) subtitulo.style.display = 'none';
        if (formBox) formBox.style.display = 'none';
        if (linksBox) linksBox.style.display = 'none';

        // ✅ Redireciona com segurança (sem UID)
        window.location.href = data.redirectTo; 
      }
      // (Se 'data.verificado' for true mas não tiver 'redirectTo', só para o loop)
      else if (data.verificado) {
         clearInterval(poll);
         if (titulo) titulo.innerText = 'E-mail verificado!';
      }

    } catch (err) {
      console.error('Erro no polling', err);
      clearInterval(poll);
    }
  }, 3000); // Pergunta a cada 3 segundos
</script>
</body>
</html>
