<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Verifique seu E-mail - Connect Skills</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <style>
    body {
      background-color: #f5f7fa;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .verificacao-box {
      background-color: white;
      border-radius: 12px;
      padding: 40px;
      box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
      text-align: center;
      max-width: 480px;
      width: 100%;
    }

    .verificacao-box h2 {
      margin-bottom: 20px;
    }

    .verificacao-box p {
      margin-bottom: 15px;
    }

    .verificacao-box .btn {
      margin-top: 15px;
    }

    .email-destino {
      font-weight: bold;
      color: #0d6efd;
    }

    .logo {
      max-width: 100px;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>

  <div class="verificacao-box">
    <% if (reenviado === 'true') { %>
        <div class="alert alert-success" role="alert">
            📧 Novo e-mail de verificação enviado com sucesso!
        </div>
    <% } %>
    <img src="/img/logo.png" alt="Logo Connect Skills" class="logo"> <!-- substitua com sua logo -->
    <h2>📨 Verifique seu e-mail</h2>
    <p>Enviamos um link de verificação para:</p>
    <p class="email-destino"><%= email %></p>

    <form action="/usuarios/reenviar-email" method="POST">
      <input type="hidden" name="email" value="<%= email %>">
      <button type="submit" class="btn btn-outline-primary w-100">🔁 Reenviar e-mail</button>
    </form>

    <p class="mt-3 text-muted">Abra sua caixa de entrada e clique no link enviado.</p>
    <p class="text-muted">Se não encontrar, verifique sua pasta de spam ou lixo eletrônico.</p>

    <a href="/login" class="btn btn-link mt-2">🔐 Ir para o login</a>
  </div>

</body>
</html>
