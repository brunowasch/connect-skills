<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Foto de Perfil - Connect Skills</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="icon" href="/img/logo.png">
  <style>
    body {
      background-color: #fff;
      font-family: 'Montserrat', sans-serif;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    .logo {
      position: absolute;
      top: 30px;
      left: 30px;
    }

    main {
      flex: 1;
    }

    .foto-icon {
      width: 160px;
      height: 160px;
      margin: 30px auto;
      display: block;
    }

    .upload-btn {
      border: 2px solid black;
      border-radius: 12px;
      padding: 10px 25px;
      margin: 10px;
      font-weight: bold;
      color: #015EDB;
      background-color: white;
      transition: 0.3s;
    }

    .upload-btn:hover {
      background-color: #015EDB;
      color: white;
    }

    footer {
      background-color: #015EDB;
      color: white;
      text-align: center;
      padding: 20px 0;
    }

    .hidden-input {
      display: none;
    }
  </style>
</head>
<body>

  <!-- LOGO -->
  <div class="logo">
    <img src="/img/logo.png" alt="Logo" style="width: 150px;">
  </div>

  <main class="d-flex flex-column align-items-center justify-content-center">
    <h3 class="text-center mt-5">Adicione uma foto de perfil</h3>

    <img src="/img/avatar.png" alt="Ícone" class="foto-icon">

    <!-- FORMULÁRIO DE UPLOAD -->
    <form action="/foto-perfil" method="POST" enctype="multipart/form-data" class="d-flex gap-3 flex-wrap justify-content-center">
      <label class="upload-btn" for="upload">Fazer upload</label>
      <input type="file" id="upload" name="foto" accept="image/*" class="hidden-input" onchange="this.form.submit()">

      <button type="button" class="upload-btn">Tirar uma foto</button>
    </form>
  </main>

  <footer>
    <p>Connect Skills © 2025 tm.<br>Todos os direitos reservados.</p>
  </footer>

</body>
</html>
