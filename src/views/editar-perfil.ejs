<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Editar Perfil - Connect Skills</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/editar-perfil.css">
  <link rel="icon" href="/img/logo.png">
</head>
<body class="bg-light">
  <%- include('partials/header') %>

  <main class="container py-5">
    <h2 class="mb-4">Editar perfil</h2>
    <form action="/editar-perfil" method="POST">
      <div class="row g-4">
        <div class="col-md-4 text-center">
          <img src="https://via.placeholder.com/100" alt="Foto" class="rounded-circle mb-3">
          <div>
            <button type="button" class="btn btn-outline-secondary btn-sm">Trocar Foto</button>
          </div>
        </div>
        <div class="col-md-8">
          <div class="mb-3">
            <label for="nome" class="form-label">Nome</label>
            <input type="text" class="form-control" id="nome" name="nome" value="<%= nome %>" required>
          </div>
          <div class="mb-3">
            <label for="sobrenome" class="form-label">Sobrenome</label>
            <input type="text" class="form-control" id="sobrenome" name="sobrenome" value="<%= sobrenome %>" required>
          </div>
          <div class="mb-3">
            <label for="localidade" class="form-label">Localidade</label>
            <input type="text" class="form-control" id="localidade" name="localidade" value="<%= localidade %>" required>
          </div>
          <div class="row">
            <div class="col-md-2 mb-3">
              <label class="form-label">DDD</label>
              <input type="text" class="form-control" value="+55" disabled>
            </div>
            <div class="col-md-2 mb-3">
              <label for="ddd" class="form-label"> </label>
              <input type="text" class="form-control" id="ddd" name="ddd" value="<%= ddd || '51' %>">
            </div>
            <div class="col-md-8 mb-3">
              <label for="telefone" class="form-label">Número</label>
              <input type="text" class="form-control" id="telefone" name="telefone" value="<%= telefone || '' %>">
            </div>
          </div>
          <div class="d-flex gap-3 mt-4">
            <button type="submit" class="btn btn-primary">Salvar</button>
            <a href="/meu-perfil" class="btn btn-danger">Cancelar</a>
          </div>
        </div>
      </div>
    </form>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
