<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Connect Skills</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
    <link rel="stylesheet" href="/css/shared/style.css"/>
    <link rel="icon" type="image/png" href="/img/CONNECT.png?v=2" />
  </head>
  <style>
  body { background: #fff; }
   .banner-wrap{
    margin-top: 0;
    line-height: 0;
    padding: 0 !important;
  }

  header, .navbar, .site-header{
    margin-bottom: 0 !important;
    padding-bottom: 0 !important;
    border-bottom: 0 !important;
  }

  .hero {
    background: #fff;
    border-bottom: 1px solid #eef1f6;
    margin-top: 0 !important;
    position: relative;
    z-index: 1;
  }
  .hero .display-6 { font-weight: 800; letter-spacing: -0.02em; }

  #carouselHome,
  #carouselHome .carousel-inner,
  #carouselHome .carousel-item { margin: 0; }

  .shadow-soft { box-shadow: 0 8px 24px rgba(16, 24, 40, 0.08); }
  .rounded-2xl { border-radius: 1rem; }
  .section-title { font-weight: 800; letter-spacing: -0.01em; }
  .btn-hero { padding: 0.75rem 1.5rem; font-size: 1.125rem; }

  header, .navbar, .site-header {
    margin-bottom: 0 !important;
    padding-bottom: 0 !important;
    border-bottom: 0 !important;
  }

  /* ===== CTA BAR ===== */
  .cta-bar { background: #0d6efd; color: #fff; border-radius: 18px; }
  .cta-bar a.btn { background: #fff; color: #0d6efd; border: none; }

  /* ===== Botões Animados ===== */
  .btn-animated {
    position: relative;
    overflow: hidden;
    transition: transform .18s ease,
                box-shadow .18s ease,
                background-color .18s ease,
                color .18s ease;
    will-change: transform;
    border-radius: 12px;
  }
  .btn-animated:hover {
    transform: translateY(-2px) scale(1.02);
    box-shadow: 0 10px 28px rgba(0,0,0,.18);
  }
  .btn-animated:active {
    transform: translateY(0) scale(.98);
    box-shadow: 0 6px 18px rgba(0,0,0,.14);
  }
  .btn-animated:focus-visible {
    outline: none;
    box-shadow: 0 0 0 4px rgba(255,255,255,.45),
                0 8px 26px rgba(0,0,0,.18);
  }

  /* Ícone desliza */
  .btn-animated .icon-move { transition: transform .18s ease; }
  .btn-animated:hover .icon-move { transform: translateX(3px); }

  /* Ripple */
  .btn-animated .ripple {
    position: absolute;
    border-radius: 50%;
    transform: translate(-50%, -50%);
    pointer-events: none;
    background: rgba(255,255,255,.45);
    width: 0; height: 0; opacity: .75;
    animation: ripple .6s ease-out forwards;
  }
  @keyframes ripple {
    to { width: 320px; height: 320px; opacity: 0; }
  }

  /* Acessibilidade */
  @media (prefers-reduced-motion: reduce) {
    .cta-bar .btn-animated,
    .cta-bar .btn-animated .icon-move {
      transition: none !important;
    }
    .cta-bar .btn-animated:hover,
    .cta-bar .btn-animated:active {
      transform: none !important;
      box-shadow: none !important;
    }
  }
</style>
  </style>
  <body id="home">
    <%- include('../partials/header') %>
    <%- include('../partials/flash-messages') %>
    <main>
      <section class="mb-0 p-0 banner-wrap">
        <div id="carouselHome" class="carousel slide" data-bs-ride="carousel">
          <div class="carousel-inner">
            <div class="carousel-item active">
              <img src="/img/Banner.png" class="d-block w-100" alt="Banner Connect Skills">
            </div>
          </div>
        </div>
      </section>
      <!-- HERO -->
      <section class="hero py-5 mt-0" data-reveal="up">
        <div class="container">
          <div class="row align-items-center g-4">
            <div class="col-lg-7">
              <h3 class="mb-2 text-secondary-emphasis fw-semibold">Bem-vindo ao Connect Skills</h3>
              <h3 class="display-6 mb-3">
                Conecte-se ao mercado de trabalho
                <span class="text-primary fs-3">com inteligência e praticidade</span>
              </h3>
              <p class="lead text-secondary mb-4">
                Cadastre-se em poucos passos ou faça login para explorar as oportunidades e publicar vagas.
              </p>
              <div class="d-flex flex-wrap gap-3">
                <a href="/cadastro" class="btn btn-primary btn-lg btn-hero">
                  <i class="bi bi-person-plus me-2"></i> Cadastrar
                </a>
                <a href="/login" class="btn btn-outline-primary btn-lg btn-hero">
                  <i class="bi bi-box-arrow-in-right me-2"></i> Login
                </a>
              </div>
            </div>
            <div class="col-lg-5 d-none d-lg-block">
              <img src="/img/hero-home.png" onerror="this.style.display='none'" class="img-fluid" alt="Ilustração home" />
            </div>
          </div>
        </div>
      </section>

      <!-- BLOCO SUBSTITUINDO “COMO FUNCIONA”: POR QUE USAR + MÉTRICAS -->
      <section class="container py-5" data-reveal="fade">
        <div class="row g-4 align-items-stretch">
          <div class="col-12 col-lg-4">
            <div class="h-100 p-4 shadow-soft rounded-2xl">
              <h2 class="section-title h3 mb-2">Por que usar o Connect Skills?</h2>
              <p class="text-secondary mb-0">
                Mais agilidade para encontrar vagas compatíveis, destacar suas soft skills e se posicionar no mercado com segurança.
              </p>
            </div>
          </div>

          <div class="col-12 col-lg-8">
            <div class="row g-4">
              <!-- Vantagem 1 -->
              <div class="col-12 col-md-4">
                <div class="h-100 p-4 shadow-soft rounded-2xl text-center">
                  <i class="bi bi-person-vcard fs-1 text-primary"></i>
                  <p class="mt-3 mb-0">
                    <strong>Perfil personalizado</strong><br>
                    Mostre suas experiências, habilidades e interesses de forma detalhada.
                  </p>
                </div>
              </div>

              <!-- Vantagem 2 -->
              <div class="col-12 col-md-4">
                <div class="h-100 p-4 shadow-soft rounded-2xl text-center">
                  <i class="bi bi-people fs-1 text-primary"></i>
                  <p class="mt-3 mb-0">
                    <strong>Conexão rápida</strong><br>
                    Encontre empresas e talentos compatíveis.
                  </p>
                </div>
              </div>

              <!-- Vantagem 3 (mantida como estava) -->
              <div class="col-12 col-md-4">
                <div class="h-100 p-4 shadow-soft rounded-2xl text-center">
                  <i class="bi bi-shield-check fs-1 text-primary"></i>
                  <p class="mt-3 mb-0">
                    Foco em <strong>soft skills</strong> e compatibilidade
                  </p>
                </div>
              </div>
            </div>
          </div>
      </section>

      <!-- CTA FINAL -->
      <section class="py-5" data-reveal="zoom" data-reveal-delay="120">
        <div class="container">
          <div class="cta-bar p-4 p-md-5 shadow-soft d-flex flex-column flex-lg-row align-items-start align-items-lg-center justify-content-between gap-3">
            <div>
              <h3 class="mb-1">Pronto para começar?</h3>
              <p class="mb-0 opacity-75">Cadastre-se ou faça login e dê o próximo passo.</p>
            </div>
            <div class="d-flex flex-wrap gap-3">
              <a href="/cadastro" class="btn btn-primary btn-lg btn-hero btn-animated">
                <i class="bi bi-person-plus me-2 icon-move"></i> Cadastrar
              </a>
              <a href="/login" class="btn btn-outline-primary btn-lg btn-hero btn-animated">
                <i class="bi bi-box-arrow-in-right me-2 icon-move"></i> Login
              </a>
            </div>
          </div>
        </div>
      </section>

      <%- include('../partials/contato', { sucesso, erro, candidato: null, empresa: null }) %>
      <%- include('../partials/footer') %>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/shared/home-reveal-on-scroll.js" defer></script>
    <script>
        document.querySelectorAll('.btn-animated').forEach(btn => {
        btn.addEventListener('click', (e) => {
          const rect = btn.getBoundingClientRect();
          const ripple = document.createElement('span');
          ripple.className = 'ripple';
          ripple.style.left = (e.clientX - rect.left) + 'px';
          ripple.style.top  = (e.clientY - rect.top)  + 'px';
          btn.appendChild(ripple);
          setTimeout(() => ripple.remove(), 650);
        }, { passive: true });
      });
    </script>
  </body>
</html>
