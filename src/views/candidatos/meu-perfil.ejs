<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Meu Perfil - Connect Skills</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/shared/meu-perfil.css">
  <link rel="stylesheet" href="/css/shared/style.css">
  <link rel="icon" href="/img/logo.png">
</head>
<body>

<main>
  <%- include('../partials/header-candidato') %>
    <div class="perfil-card bg-white p-4 rounded shadow-sm">
      <div class="d-flex align-items-center perfil-header mb-4">
        <img src="<%= fotoPerfil || 'https://via.placeholder.com/80' %>" alt="Foto" class="perfil-foto rounded-circle me-3">
        <div>
          <h4 class="mb-1"><%= nome || 'Usuário' %> <%= sobrenome || '' %></h4>
          <p class="mb-1"><%= localidade || '' %></p>
          <small class="d-block">+55 <%= ddd || '51' %> <%= telefone || 'xxxxxxxxxx' %></small>
          <small class="d-block">Data de nascimento: <%= dataNascimento || 'não informada' %></small>
        </div>
        <a href="/candidato/editar-perfil" class="btn btn-primary btn-sm ms-auto">Editar perfil</a>
      </div>

      <div class="row gy-4">
        <div class="col-md-6 col-12">
          <h5 class="d-flex justify-content-between align-items-center">
            Áreas de interesse
            <a href="/candidato/areas" class="text-decoration-none" title="Editar áreas">
              <i class="bi bi-pencil-square fs-6"></i>
            </a>
          </h5>
          <ul class="list-group">
             <%= console.log(areas) %>
            <% if (areas && areas.length) { %>
              <% areas.forEach(area => { %>
                <li class="list-group-item"><%= area %></li>
              <% }) %>
            <% } else { %>
              <li class="list-group-item">Nenhuma área selecionada.</li>
            <% } %>
          </ul>
        </div>

        <!-- <div class="col-md-6 col-12">
          <h5>Minhas candidaturas</h5>
          <ul class="list-group">
            <li class="list-group-item d-flex justify-content-between align-items-center">
              Curadoria Digital - Gerente de produtos
              <a href="#" class="btn btn-outline-primary btn-sm">Acompanhar processos</a>
            </li>
          </ul>
        </div> -->
      </div>
    </div>
  </main> 

  <%- include('../partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
