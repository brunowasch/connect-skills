<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Connect Skills</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/shared/style.css">
  <link rel="stylesheet" href="/css/shared/telefone.css">
  <link rel="icon" type="image/png" href="/img/CONNECT.png?v=2">
</head>
  <style>
    :root { --nav-h: 0px !important; }
    body#cadastro-telefone { margin: 0; padding-top: 0 !important; }
  </style>
<body id="cadastro-telefone">
  <%- include('../partials/header') %>
  <%- include('../partials/flash-messages') %>
  <main class="d-flex flex-column align-items-center justify-content-center mt-5">
    <h3 class="text-center mb-4">Digite seu número de telefone:</h3>

    <form action="/candidatos/telefone" method="POST" id="formTelefone" novalidate>
      <input type="hidden" name="uid" value="<%= uid %>">
      <input type="hidden" name="ddi" value="+55">

      <% if (error) { %>
        <div class="alert alert-danger text-center"><%= error %></div>
      <% } %>

      <div class="form-card">
        <div class="mb-4">
          <div class="d-flex justify-content-center align-items-center gap-2 flex-wrap">
            <span class="border border-dark px-2 py-1 rounded-pill">+55</span>
            <div class="d-flex flex-column align-items-start">
              <input type="text" class="form-control text-center" name="ddd" id="ddd"
                     maxlength="2" placeholder="DDD" style="width: 70px;" required>
              <div class="invalid-feedback text-danger">Informe um DDD válido com 2 dígitos.</div>
            </div>
            <div class="d-flex flex-column align-items-start">
              <input type="text" class="form-control text-center" name="telefone" id="telefone" maxlength="11" placeholder="Número" style="width: 180px;" required>
              <div class="invalid-feedback text-danger">Informe um número válido entre 8 e 11 dígitos.</div>
            </div>
          </div>
        </div>

        <div class="d-grid">
          <button type="submit" class="btn btn-primary btn-lg">Continuar</button>
        </div>

        <div class="text-center mt-3">
        </div>
      </div>
    </form>
    <form action="/usuarios/pular-cadastro" method="POST" class="d-inline">
      <button type="submit" class="btn btn-link text-decoration-none">
        Prefiro completar meu perfil depois
      </button>
    </form>
  </main>

  <%- include('../partials/footer') %>
  <script src="/js/shared/numeroTelefone.js"></script>
</body>
</html>