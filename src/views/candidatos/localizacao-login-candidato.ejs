<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Connect Skills</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/shared/style.css">
  <link rel="stylesheet" href="/css/shared/localizacao-login.css">
  <link rel="icon" type="image/png" href="/img/CONNECT.png?v=2">
</head>
  <style>
    :root { --nav-h: 0px !important; }
    body#cadastro-localizacao { margin: 0; padding-top: 0 !important; }
    .main-ajustado-header { margin-top: 0 !important; padding-top: 0 !important; }
  </style>
<body  id="cadastro-localizacao" class="d-flex flex-column min-vh-100">

  <%- include('../partials/header') %>
  <%- include('../partials/flash-messages') %>
   <main class="main-ajustado-header flex-grow-1 d-flex flex-column justify-content-center align-items-center text-center px-3">
    <h1 class="mb-4">Para saber as melhores oportunidades, digite sua região:</h1>

    <div class="form-container">
      <form id="formLocalizacao" action="/candidatos/localizacao" method="POST" novalidate>
        <input type="hidden" name="uid" value="<%= uid %>">

        <div class="mb-3 text-start">
          <label for="localidade" class="form-label">Localidade</label>
          <div class="position-relative">
            <input type="text" class="form-control" id="localidade" name="localidade"
                   autocomplete="off" placeholder="Cidade, Estado, País" required>
            <ul id="sugestoesLocalidade"
                class="list-group position-absolute w-100 d-none"
                style="z-index: 1000;"></ul>
          </div>

          <button type="button" class="btn btn-outline-secondary mt-2 w-100 text-center" id="btnLocalizacao">
            <i class="bi bi-geo-alt-fill me-2"></i> Usar minha localização
          </button>
          <button type="button" class="btn btn-link mt-2 w-100 text-center text-decoration-none d-none"
                  id="btnEditarLocal">
            <i class="bi bi-pencil-square"></i> Editar localização
          </button>

          <div id="erro-localidade" class="invalid-feedback d-none mt-2">
            Informe sua localidade para continuar.
          </div>
        </div>

        <div class="d-grid">
          <button type="submit" class="btn btn-primary btn-lg">Continuar</button>
        </div>
        <div class="text-center mt-3">
          <form action="/usuarios/pular-cadastro" method="POST" class="d-inline">
            <button type="submit" class="btn btn-link text-decoration-none">
              Prefiro completar meu perfil depois
            </button>
          </form>
        </div>
      </form>
    </div>
  </main>

  <%- include('../partials/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/shared/localizacaoAPI.js"></script>
</body>
</html>
