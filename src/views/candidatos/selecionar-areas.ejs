<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />
  <title>Áreas de Interesse - Connect Skills</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/shared/style.css">
  <link rel="stylesheet" href="/css/candidatos/selecionar-areas.css">
  <link rel="icon" href="/img/CONNECT.png">
</head>
<body>
  <%- include('../partials/header') %>

<main class="container text-center mt-5 pt-5">
  <h2 class="mb-4">Selecione a seguir três áreas a quais você deseja trabalhar:</h2>

  <form action="/candidato/cadastro/areas?usuario_id=<%= usuario_id %>" method="POST" id="formAreas">
    <input type="hidden" name="usuario_id" value="<%= usuario_id %>">
    <input type="hidden" name="areasSelecionadas" id="areasSelecionadas">

    <div id="botoes" class="d-flex flex-wrap justify-content-center">

      <% if (areas && areas.length > 0) { %>
        <% areas.forEach((area) => { %>
          <button type="button" class="area-btn btn btn-outline-primary btn-sm m-2" data-value="<%= area.nome %>">
            <%= area.nome %>
          </button>
        <% }) %>
      <% } else { %>
        <p class="text-muted">Nenhuma área disponível no momento.</p>
      <% } %>

      <!-- Botão "Outro" -->
      <button type="button" class="area-btn btn btn-outline-secondary btn-sm m-2" data-value="Outro">Outro...</button>
    </div>

    <!-- Campo para inserir nova área -->
    <div id="campoOutroArea" class="mt-3 d-none">
      <label for="outra_area" class="form-label">Descreva a outra área:</label>
      <input
        type="text"
        id="outra_area"
        name="outra_area_input"
        class="form-control form-control-sm mx-auto mb-2"
        style="max-width: 400px;"
        placeholder="Digite a nova área"
      />
      <div class="d-flex justify-content-center gap-2">
        <button type="button" id="btnConfirmarOutro" class="btn btn-primary btn-sm" disabled>Confirmar</button>
        <button type="button" id="btnCancelarOutro" class="btn btn-secondary btn-sm">Cancelar</button>
      </div>
    </div>

    <div class="mt-4">
      <button type="submit" class="btn btn-continuar mt-4">Continuar</button>
    </div>
  </form>
</main>

<%- include('../partials/footer') %>
<script src="/js/candidatos/selecionar-areas.js"></script>
</body>
</html>
