<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Vagas Disponíveis - Connect Skills</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <link rel="stylesheet" href="/css/vagas.css"> 
  <link rel="icon" href="/img/logo.png">
</head>

<%- include('../partials/header-candidato-vagas') %>
<body>
    <div class="container mt-5">
    <h2 class="mb-4 mt-5">Vagas disponíveis</h2>

    <% if (vagas.length > 0) { %>
        <% vagas.forEach(vaga => { %>
        <div class="card mb-3 p-3">
            <div class="d-flex align-items-center mb-2">
            <img src="<%= vaga.empresa.logo %>" alt="Logo" class="me-3" width="50" height="50" style="border-radius: 50%;">
            <h5 class="mb-0"><%= vaga.empresa.nome %> - <%= vaga.cargo %> (<%= vaga.tipo %>)</h5>
            </div>
            <p><strong>Descrição:</strong> <%= vaga.descricao %></p>
            <p><strong>Áreas:</strong> <%= vaga.areas.join(', ') %></p>
            <p><strong>Habilidades:</strong> <%= vaga.habilidades.join(', ') %></p>
            <small class="text-muted">Publicada em: <%= vaga.data %></small>
        </div>
        <% }) %>
    <% } else { %>
        <p>Não há vagas disponíveis no momento.</p>
    <% } %> 
    </div>
</body>
</html>